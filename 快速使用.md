# 快速使用指南

迈克尔逊干涉仪智能辅助系统 — 从 clone 到完成第一次实验。

## 1. 获取项目

```powershell
git clone https://github.com/Zhang-Zhi-b-o/AI_Interferometry.git
cd AI_Interferometry
```

> 仓库已包含全部模型权重（YOLO `.pt` + PP-OCR `.onnx`，共约 90MB），无需额外下载。如果 clone 速度慢，可以加 `--depth 1` 只拉最新版本。

## 2. 安装环境

**需要**：Windows 10+、Python 3.10+。

```powershell
# 创建虚拟环境
python -m venv .venv

# 激活（PowerShell）
.\.venv\Scripts\Activate.ps1

# 如果报"无法加载文件"，先执行：
# Set-ExecutionPolicy -Scope Process Bypass
# .\.venv\Scripts\Activate.ps1

# 安装依赖
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

依赖包括：`opencv-python`、`numpy`、`scipy`、`pillow`、`pyserial`、`pyyaml`、`ultralytics`、`torch`、`rapidocr`、`onnxruntime`。首次安装可能需要几分钟。

## 3. 配置密钥

实验助手需要 DeepSeek API。在项目根目录新建 `config/secrets.yaml`：

```yaml
deepseek_api_key: "你的 DeepSeek API Key"
```

> 这个文件已被 `.gitignore` 排除，不会提交到 Git。
>
> 如果没有密钥，也可以跳过此步骤——助手会使用本地知识库做降级回答。

也可以设置环境变量（优先级更高）：

```powershell
$env:DEEPSEEK_API_KEY="你的 DeepSeek API Key"
python run.py
```

## 4. 硬件准备

| 设备 | 用途 | 备注 |
|---|---|---|
| USB 摄像头 ×2 | 干涉画面 + 微分表读数 | 分别默认索引 1 和 0，可在界面切换 |
| CH340 步进电机 | 驱动干涉仪动镜 | 串口自动检测 |
| 迈克尔逊干涉仪 | 实验主体 | 白光 + 红光光源 |
| Arduino（可选）| 激光测距辅助 | |

首次建议**电机空载**测试，确认方向、停止命令正确后再连接机械负载。

## 5. 启动

```powershell
python run.py
```

界面包含两个页面：「手动操作」和「自动实验」。

## 6. 运行测试（无需硬件）

```powershell
python -m unittest discover -s tests -v
```

12 个测试覆盖配置、智能体、控制状态机、安全校验、视觉算法等，全部不依赖真实硬件。

## 7. 实验流程

### 手动操作页

| 步骤 | 操作 |
|---|---|
| 1. 打开摄像头 | 选择相机索引，点击「打开摄像头」 |
| 2. 画面矫正 | 旋转、缩放使条纹水平，拖拽框选 ROI |
| 3. 加载模型 | 选择 YOLO 权重，点击「加载模型」 |
| 4. 连接电机 | 选择串口，点击「连接电机」 |
| 5. 手动调光 | 用红光调整干涉仪使条纹清晰 |

### 自动实验页

| 步骤 | 操作 |
|---|---|
| 6. 切换到「自动实验」 | —— |
| 7. 勾选两个确认项 | 红光调整完成 + 白光光源已放置 |
| 8. 打开「自动进行实验」 | 程序接管后续操作 |

程序将自动：启动相机/YOLO/电机/微分表 → 双向扩展搜索白光中心条纹 → 接近时减速 → 中心稳定后停车 → 记录坐标和微分表读数。

随时关闭「自动进行实验」或点击「紧急停车」即可停止自动流程。

## 8. 常见问题

| 问题 | 解决方法 |
|---|---|
| 摄像头打不开 | 检查索引号，确认没被其他程序占用 |
| 串口连不上 | 安装 CH340 驱动，检查设备管理器中的端口号 |
| YOLO 加载失败 | 确认 `models/current/` 下有 `.pt` 文件 |
| 助手不回答 | 检查 `config/secrets.yaml` 的 Key 或网络 |
| 自动寻中不停 | 点「紧急停车」或关掉自动实验开关 |
| 微分表读数异常 | 调整微分表相机 ROI，确保 LCD 完整可见 |
| 模型推理很慢 | 检查 PyTorch 是否为 CUDA 版本（CPU 也可用，较慢） |

## 9. 项目文件说明

| 文件/目录 | 用途 |
|---|---|
| `run.py` | 启动入口 |
| `config.yaml` | 相机、YOLO、电机等运行参数 |
| `config/secrets.yaml` | **需自建**，填写 API Key |
| `config/calibration.yaml` | 实验标定数据 |
| `models/current/` | 当前使用的 YOLO 权重 |
| `models/micrometer/` | PP-OCR 微分表识别模型 |
| `src/agent/knowledge_base/` | 本地实验知识库 |
| `快速使用.md` | 本文件 |
| `README.md` | 完整说明文档 |
| `ARCHITECTURE.md` | 系统架构和数据流 |

---

克隆后即开即用，只需补一个密钥文件。
