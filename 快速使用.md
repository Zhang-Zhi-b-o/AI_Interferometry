# 快速使用指南

迈克尔逊干涉仪智能辅助系统 — 从零开始到完成一次实验。

## 环境准备

**系统要求**：Windows 10+、Python 3.10+、至少两个 USB 摄像头、CH340 串口电机控制器。

```powershell
cd D:\Files\Work\光电\AI_Interferometry
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

> 如果 PowerShell 禁止执行激活脚本：
> ```powershell
> Set-ExecutionPolicy -Scope Process Bypass
> .\.venv\Scripts\Activate.ps1
> ```

## 配置

### 1. DeepSeek API 密钥（实验助手需要）

在 `config/secrets.yaml` 中填写：

```yaml
deepseek_api_key: "你的 DeepSeek API Key"
```

或者启动前设置环境变量：

```powershell
$env:DEEPSEEK_API_KEY="你的 DeepSeek API Key"
```

未配置密钥时，助手使用本地知识库降级回答。

### 2. 相机和串口

- 默认干涉相机索引为 `1`，微分表相机索引为 `0`（可在界面中切换）
- 电机串口自动检测 CH340 设备，也可手动选择
- 详细参数见 `config.yaml`

## 启动

```powershell
python run.py
```

## 快速实验流程

### 手动操作页

1. **打开摄像头** → 选择相机索引，点击"打开摄像头"
2. **画面矫正** → 旋转、缩放使干涉条纹水平，拖拽框选 ROI
3. **加载模型** → 选择 YOLO 权重文件，点击"加载模型"
4. **连接电机** → 选择串口，点击"连接电机"
5. **手动调光** → 用红光调整干涉仪，使条纹清晰

### 自动实验页

6. **切换到"自动实验"页**
7. 勾选"仪器已用红光调整完成"和"白光光源已放置完成"
8. 打开"自动进行实验"开关

程序将自动：
- 检查并启动相机、YOLO、电机和微分表
- 双向搜索白光中心条纹
- 接近中心时自动减速
- 中心稳定后停车，记录坐标和微分表读数

### 结束

- 关闭"自动进行实验"可随时停止自动流程
- 关闭窗口前确认电机已停止

## 运行测试

```powershell
python -m unittest discover -s tests -v
```

所有测试无需硬件，可在任何环境运行。

## 常见问题

| 问题 | 解决方法 |
| --- | --- |
| 摄像头无法打开 | 检查相机索引，确认未被其他程序占用 |
| 串口连接失败 | 确认 CH340 驱动已安装，检查串口号 |
| YOLO 模型加载失败 | 确认 `models/current/` 中有 `.pt` 权重文件 |
| 助手无法回答 | 检查 `config/secrets.yaml` 中的 API Key 或网络连接 |
| 自动寻中不停 | 点击"紧急停车"或关闭自动实验开关 |
| 微分表读数异常 | 调整微分表相机 ROI，确保 LCD 完整可见 |

详细说明见 [README.md](README.md)，系统架构见 [ARCHITECTURE.md](ARCHITECTURE.md)。
