# 新功能使用指南

本文介绍实验助手从「只回答」升级为「能动手 + 会主动提醒」后的四项新能力，
以及新增的单帧薄膜厚度分布（不均匀度）分析与 `michelson` 命令行。每节先讲
**是什么**，再讲**怎么用**。

- [一、实验助手自主执行（能动手，需确认）](#一实验助手自主执行能动手需确认)
- [二、DeepSeek 主动建议（停下来会提醒）](#二deepseek-主动建议停下来会提醒)
- [三、薄膜厚度分布（不均匀度）分析](#三薄膜厚度分布不均匀度分析)
- [四、命令行工具 michelson](#四命令行工具-michelson)
- [五、相关配置](#五相关配置)

---

## 一、实验助手自主执行（能动手，需确认）

### 是什么

以前助手只能「你问我答」。现在勾上「自主执行」后，它会像搭档一样**自己干活**：
接到任务后先**定计划**，再**一步步调用工具**（读表、查状态、测条纹、算厚度/误差，
或经你确认后驱动电机），最后**汇报结果**。界面会实时显示它的计划和每一步动作。

### 怎么用

1. 打开「**04 实验助手**」浮窗；
2. 勾选状态栏顶部的「**自主执行**」（默认已勾）；想看它怎么规划而不真动手，可勾「仅规划」；
3. 在输入栏下达一句话任务，例如：

   > 调出中心条纹并测量条纹宽度
   > 读一下微分表当前读数，算这块玻璃的厚度
   > 对当前画面做一次厚度分布分析

4. 回车（或 `Ctrl+Enter`）发送，观察状态栏里的「计划」与「工具活动」逐条更新；
5. 若助手要动电机，浮窗顶部会弹出「**确认执行 / 拒绝**」——点确认才执行，点拒绝或超时则它改道。

随时可点红色的「**急停**」立即停电机。

### 安全分级

| 级别 | 工具类型 | 是否需确认 |
| --- | --- | --- |
| 📖 只读 | 读表、查状态、测条纹、算厚度/误差、记计划 | 自动执行 |
| 🔧 运动 | 自动寻中、目标读数测量、回程差测量 | **需人工确认** |
| ⛔ 停止 | 急停及各类停止 | 始终放行 |

> 助手只编排「高层操作」（开/停寻中、测量、单帧分析），**不会逐拍驱动电机**；
> 底层 PID、寻中、停车确认仍由 GUI 现有实时循环安全执行。

### 可调用的工具清单

| 工具 | 作用 | 级别 |
| --- | --- | --- |
| `get_context` | 读取当前运行时状态快照 | 只读 |
| `micrometer_read` | 读数显微分表当前读数（mm） | 只读 |
| `motor_status` | 查电机运行/档位/方向/寻中状态 | 只读 |
| `fringe_center_status` | 查中心条纹定位与是否居中 | 只读 |
| `fringe_width_analyze` | 测中心条纹宽度 | 只读 |
| `thickness_analyze` | 单帧薄膜厚度分布估计 | 只读 |
| `sample_colour` | 采样薄膜区域中位颜色 (r,g,b) | 只读 |
| `glass_thickness_calculate` | 由两次读数算玻璃片厚度 | 只读 |
| `uncertainty_analyze` | GUM 不确定度评定 | 只读 |
| `experiment_session_stats` | 查会话统计（轮数、摘要） | 只读 |
| `set_plan` | 把分步计划写到界面 | 只读 |
| `record_note` | 记一条实验备注 | 只读 |
| `auto_center_start` | 启动自动寻中 | **运动** |
| `measurement_start` | 启动目标读数测量 | **运动** |
| `backlash_measure` | 启动回程差测量 | **运动** |
| `motor_emergency_stop` | 立即急停电机 | 停止 |
| `auto_center_stop` | 停止自动寻中 | 停止 |
| `measurement_stop` | 停止目标读数测量 | 停止 |
| `backlash_stop` | 停止回程差测量 | 停止 |

---

## 二、DeepSeek 主动建议（停下来会提醒）

### 是什么

你**一段时间不操作**（默认 45 秒）后，状态栏的「主动建议」一行会自动出现 DeepSeek
的分析结果，形如：

> 现状：… ｜ 下一步：… ｜ 其他建议：…

它只在空闲时分析、回答很短，因此 token 消耗很低；离线或调用失败时自动退回
本地确定性提示（来源标注「本地提示」）。

### 怎么用

不需要任何操作——正常操作时不会被打扰，**停下来一会儿**它就会自己出现。想改触发
间隔或回答长度，见「[五、相关配置](#五相关配置)」。

---

## 三、薄膜厚度分布（不均匀度）分析

### 是什么

给一帧彩色干涉图，就能算出整片薄膜**哪里厚、哪里薄**，输出一张厚度伪彩叠加图和
一组不均匀度指标（RMS 不均匀度、峰谷值 PV 等）。有两种模式：

- **相对模式**（默认）：按颜色级次解包相位估算厚度，模型依赖，不是可溯源绝对厚度；
- **标定模式**：填入「颜色 → 光程差」标定 CSV，把像素颜色匹配到已扣除基准的
  光程差后换算，接近绝对厚度。

### 怎么用（GUI）

1. 打开「**05 临时测量 → 薄膜厚度分布（单帧）**」面板；
2. 确认画面里已有清晰的彩色干涉条纹；
3. 填参数（一般留默认即可）：
   - **波长 (nm)**：默认 `589.3`；
   - **折射率**：默认 `1.523`；
   - **标定 CSV**：留空为相对模式；填入路径则用标定模式；
   - 需要时可勾「反转厚度方向」，或先点「捕获无膜基准图」扣除系统固有分布；
4. 点「**分析单帧厚度分布**」，稍等即可在下方看到指标和伪彩叠加图。

### 输出指标

| 指标 | 含义 |
| --- | --- |
| 模式 | 相对 / 标定 |
| 有效像素 | 参与计算的薄膜像素数 |
| 稳健最小值 (2%) / 最大值 (98%) | 去掉两端极值的厚度范围 |
| 稳健峰谷值 PV | 最大值减最小值 |
| **RMS 不均匀度** | 厚度的均方根起伏，越小越均匀 |
| 中间 90% 跨度 | 5% 到 95% 分位之间的厚度跨度 |
| 中位置信度 | 逐像素估计的平均可信度 |

### 怎么用（命令行）

```powershell
michelson analyze thickness-distribution 图片.jpg `
  --wavelength 589.3 --refractive 1.523 --calibration calib.csv --baseline baseline.jpg
```

---

## 四、命令行工具 michelson

### 是什么

与 GUI 智能体共用同一批纯逻辑能力的命令行，可脱离界面在终端运行。硬件子命令
（运动/急停/读实时表）需在 GUI 内运行，CLI 不另建无头硬件连接。

> 已注册 `michelson` 命令；未安装时也可用 `python -m src.cli ...` 运行。

### 子命令一览

| 命令 | 作用 |
| --- | --- |
| `michelson measure glass-thickness d1 d2 [--n]` | 由两次微分表读数算玻璃片厚度 |
| `michelson measure uncertainty 值... [--d1] [--d2] [--n]` | 玻璃片厚度不确定度评定 |
| `michelson analyze fringe-width 图片 [--center-x]` | 分析图像中心条纹宽度 |
| `michelson analyze thickness-distribution 图片 [...]` | 单帧薄膜厚度分布估计 |
| `michelson control center-search [...]` | 自动寻中状态机（纯模拟） |
| `michelson tools` | 列出工具注册表（含风险分级） |

### 示例

```powershell
# 算玻璃片厚度（d1=1.000, d2=1.500, n=1.4586）
michelson measure glass-thickness 1.000 1.500 --n 1.4586

# 不确定度评定（厚度序列 + 对应 d1/d2 读数）
michelson measure uncertainty 1.23 1.24 1.22 `
  --d1 1.000 1.001 0.999 --d2 1.500 1.501 1.499

# 测条纹宽度
michelson analyze fringe-width C:\path\to\fringe.jpg --center-x 640

# 列出所有可用工具（供人核对与 AI 一致的视图）
michelson tools
```

---

## 五、相关配置

在 `config.yaml` 的 `agent` 分组（改完需重启程序）：

| 键 | 默认 | 含义 |
| --- | ---: | --- |
| `autonomous_enabled` | `true` | 开启后提问走自主执行，否则退回纯文本引导 |
| `confirm_motion` | `true` | 运动工具是否需界面人工确认 |
| `dry_run` | `false` | 仅规划模式：工具只返回占位结果，不实际执行 |
| `max_steps` | `12` | 单次任务最多工具调用轮次 |
| `tool_timeout_seconds` | `60` | 运动工具确认框等待超时（超时视为拒绝） |
| `suggestion_idle_seconds` | `45` | 无操作多久后自动分析主动建议（越大越省 token） |
| `suggestion_max_tokens` | `200` | 主动建议输出上限（短答） |

DeepSeek 密钥保存在不会提交 Git 的 `config/secrets.yaml`（或用环境变量
`DEEPSEEK_API_KEY`）。未配置密钥时，自主执行模式不可用（依赖 function calling），
会自动退回纯文本引导；本地知识库问答与确定性计算不受影响。
