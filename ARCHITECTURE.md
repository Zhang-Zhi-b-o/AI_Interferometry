# 系统架构

## 设计目标

本项目是迈克尔逊白光干涉实验辅助系统。它将相机采集、YOLO 条纹识别、中心条纹定位、步进电机寻零和只读实验助手组合在一个 Tkinter 桌面应用中。

核心约束：

- Tkinter 主线程只负责界面和状态消费，不执行可能阻塞的串口、模型推理或在线请求。
- 自动控制采用确定性状态机；实验助手没有硬件控制权限。
- 电机命令必须串行执行，停止命令优先于普通查询和调速。
- 配置错误在启动阶段集中报告，不在运行时静默使用危险默认值。

## 分层结构

```text
UI 层
  src/ui/app.py                  组装、事件路由、画面呈现
  src/ui/widgets/               独立 Tkinter 面板
        │
应用服务层
  src/agent/session.py          可取消的守护会话
  src/control/center_control.py 双向自动寻中状态机
  src/control/experiment_workflow.py 实验进度识别与自动推进状态机
  src/hardware/command_queue.py 串口后台命令队列
        │
领域与设备层
  src/agent/                    RAG、提示词、DeepSeek Provider
  src/vision/                   YOLO、ROI、角度、中心条纹
  src/hardware/                电机协议、Arduino 数据
  src/camera/                  OpenCV 相机
        │
基础设施
  src/config.py                 YAML 加载与范围校验
  src/logging.py                控制台和本地文件日志
```

`src/ui/app.py` 仍是唯一组装点，但串口调度、自动控制转换和助手生命周期已经拆成可独立测试的组件。后续新增功能应优先进入对应服务，不再继续扩张主控制器。

## 主要数据流

### 相机和视觉

```text
CameraManager.read
  → 旋转、缩放和平移
  → 后台 YOLODetector.detect
  → UI 消费检测结果
  → 扩大零级框邻域并定位中心条纹
  → 连续帧判断条纹存在性与向左/向右移动
  → 自动控制状态机 / 画面显示 / 智能体状态快照
```

模型类别不能使用固定 ID。当前兼容层根据真实 `model.names` 将 `zero_order` 归为黑条/零级语义，将 `near_fringe`、`far_fringe` 归为彩条语义。
中心二次定位的扩展倍数、搜索半径和零级框边界余量由 `config.yaml` 的
`vision.center_search_*` 参数控制；扩大范围后仍使用 YOLO 零级框作为位置先验，
避免把远处普通彩条误判为中心条纹。

### 电机控制

```text
UI 参数 + 非中心条纹框 + 中心条纹横坐标 + 画面宽度
  → CenterControlStateMachine.update
  → 产生 set_speed/start_forward/start_reverse/stop 命令
  → SerialCommandQueue 单线程顺序执行
  → UI 定时领取 CommandResult
```

状态机不引用 Tkinter、串口或真实时间，因此测试可以注入虚拟时间。非中心框
引导采用“上一位置连续匹配 → 多帧中值平滑 → 线索方向确认 → 搜索中心平移 → 本轮锁向”流程。
线索存在期间同样积分电机估算位置并扩大已搜索边界；线索抖动、远离或消失不再
直接触发换向。只有到达规划边界或识别到真实中心条纹后才允许改变方向。测试覆盖
线索区间累计、动态搜索重心、旧区间锁向、路标连续性、抖动抑制、扩展双向扫描、方向学习、
接近中心减速、稳定停车、超时、条纹丢失和串口失联。手动电机位于“手动操作”页；
自动寻中和完整实验流程集中在独立的“自动实验”页，自动寻中参数也可由完整流程复用。

`ExpandingSearchPlanner` 以启动点为初始虚拟零点，根据档位标称
每转耗时积分电机估算圈数，维护 `[searched_min, searched_max]` 已覆盖区间和下一
目标边界。稳定线索可以把搜索中心向建议方向平移；规划器先到达新中心，再围绕它
左右交替扩散，同时保留全局已覆盖区间。到达边界后先快速穿过已覆盖区间，再进入另一侧新区间，并按配置倍数
扩大半径。最大范围为 `0` 时规划器使用无上限扩展模式，不产生“范围已覆盖”停车；
每完成一轮双侧覆盖便将搜索档位降低指定级数（档位数字越小越快），直到配置的
最快档位。运行超时、串口失联和人工停止仍具有更高优先级。该位置是开环估计，
不能替代编码器或限位器。

中心检测使用“候选 → 连续确认 → 已确认”三级状态。未经连续确认的单帧中心结果
丢失时不会触发停车；已确认中心短时丢失时先停车等待，出现非中心线索立即恢复
范围搜索，等待超过配置帧数后也会清除中心锁定并继续扩展扫描。只有达到中心容差
并满足稳定帧数，或触发运行超时、串口失联、人工停止时，自动任务才真正结束。

### 实验助手

```text
用户问题 + UI 主线程快照的实验状态
  → 本地 Markdown 检索（最多 top_k 块）
  → 最近 4 轮受限历史
  → DeepSeek 流式生成
  → AgentSession 将结果交回 UI
```

助手请求运行在守护线程中，支持停止生成；关闭窗口时会设置取消事件，不会因 30 秒网络超时阻止进程退出。知识来源只用于内部约束，聊天回答不追加来源列表。

### 智能实验流程与视觉微分表

```text
人工确认红光调光 + 人工确认白光放置
  → 第二摄像头画面 → LCD 定位 → PP-OCR → 连续帧稳定读数
  → ExperimentWorkflowStateMachine 识别阶段
  → 程序检查相机 / YOLO / 电机
  → 自动启动条纹搜索
  → 双向旋转将中心条纹移动到画面中央，接近时减速
  → 中心位置连续稳定
  → 优先停车并记录中心坐标与微分表读数
  → 实验助手读取阶段和下一步并进行解释
```

`MicrometerReader` 独占第二摄像头并在后台线程采集，`MicrometerOCR` 自动定位 LCD、调用本地 PP-OCRv6 ONNX 识别模型，并经过格式检查与连续帧确认后才发布读数。UI 线程只消费最新结果。自动流程分别保存启动参考读数和中心稳定读数，动镜位移按“读数变化 × 机构放缩系数”计算；每帧 OCR 未稳定时会清空流程输入，避免把旧读数误当作停车后的新读数。

自动实验页通过 `AutomaticExperimentDashboard` 同时镜像干涉画面和微分表画面，并以约 150 ms 周期刷新阶段、进度、10分钟计时、设备状态、中心误差、搜索状态、读数和位移。自动准备复用手动模块的相机、模型、串口、微分表和录像接口；紧急停车直接关闭流程状态机、停止自动寻中和自动启动的录像。

`AutomaticDeviceDebugPanel` 不创建第二套硬件控制器，而是把自动页选择的相机索引、串口和图像参数转发给 `YoloCamApp` 中已有的手动模块接口。自动页 ROI 预览会把 500×280 字幕画面的拖拽坐标反算到校正后原始帧坐标；缩放、旋转和平移直接更新共享的 `FrameCorrector`，因此手动页、自动预览、YOLO 推理和录像保持一致。

## 生命周期与安全

- 关闭摄像头或模型插件时停止预测和自动控制。
- 自动控制达到最大运行时间、连续丢失条纹或串口失联时生成优先停止命令。
- 关闭窗口时依次停止干涉预览、微分表采集、录制、预测、电机轮询和助手会话。
- 串口队列是守护线程；关闭时优先执行电机停止与断开操作。
- `config/secrets.yaml` 只保存在本地并由 Git 忽略。

## 验证边界

无硬件测试覆盖配置校验、本地检索、助手取消、类别映射、ROI 边界、中心条纹输入、电机协议、命令队列和自动控制状态转换。真实设备上线前仍需依次执行相机单测、电机空载、离线视频推理和低速闭环测试。
