# AI Interferometry 项目厚度分布分析功能技术报告

> 审阅对象：`D:\Files\Work\光电\AI_Interferometry`
>
> 审阅基线：Git `main`，提交 `d4cfba3`
>
> 审阅日期：2026-08-31
> 报告性质：基于当前仓库代码、测试、样例输出和实验记录的静态审阅与本地可复现实验；未进行真实仪器计量验证。

## 1. 结论摘要

项目中存在两套同源的“单帧彩色干涉图厚度分布”实现：

1. 独立测试项目：`Thickness_variation_analysis/analyze.py`；
2. 已集成实现：`src/vision/thickness_distribution.py`，并接入 GUI、`michelson` CLI 和实验智能体工具。

两者的核心思路相同：先分割样品区域，把 Lab 色彩空间中的色度残差转换为包裹相位，再做二维相位展开，以一个颜色周期对应一个干涉级次，将相位换算为相对厚度。可选“颜色—位移/光程差”CSV 标定模式通过 Lab 最近邻匹配生成逐像素标定量。

当前功能可以作为**定性或半定量的表面厚薄趋势可视化工具**，但还不能作为经过验证的绝对厚度计量功能。主要原因如下：

- 默认模式的“色彩相位”是经验构造量，不是由多波长干涉正演模型反演得到的物理相位；其绝对值、符号和跨周期连续性都依赖图像条件及二维解包结果。
- 代码会把每张厚度图强制减去其中位数，因此即使使用标定 CSV，未再提供绝对厚度锚点时，输出仍是零中心的空间起伏，而不是绝对厚度。
- 当前标定模式存在可复现的运行时错误：`distance` 是一维数组，却用二维 `mask` 索引，实际调用会抛出 `IndexError`。独立版和集成版均有同一问题。
- `opd_um` 的物理命名与换算口径不统一。独立版把它解释为双程附加光程差并除以 `2(n-1)`；集成版把同名字段除以 `(n-1)`。结合项目的 20:1 传动关系，GUI 自动产生的数值实际上等于动镜位移，而不是通常定义下的双程 OPD。
- GUI 自动标定使用绝对值 `|当前读数−零点|`，会丢失扫描方向；而仓库的正式标定表含正、负两侧数据。颜色又具有周期性，仅凭单像素颜色做全局最近邻匹配，不能可靠区分相同/相近颜色所在的不同级次。
- “置信度”是按单幅图自身第 90 百分位归一化的启发式图像质量分数，不是概率，也不是测量不确定度。
- 无绝对厚度锚定时，百分比不均匀度仍可能被计算。两张仓库样例的当前集成结果分别给出 `PV=2246.97%` 和 `PV=23690.05%`，这些数值来自接近零的分母，没有物理解释意义。

因此，现阶段最稳妥的产品表述是：

> 相对模式输出“由单帧颜色纹理推断的模型依赖相对形貌”；标定模式在修复运行错误并统一量纲后，可输出“经实验标定的相对厚度起伏”。只有加入可追溯的绝对厚度锚定、标定不确定度、配准和实物标准验证后，才可称为绝对厚度分布测量。

## 2. 功能范围与代码版图

### 2.1 独立测试项目

| 文件 | 作用 |
|---|---|
| `Thickness_variation_analysis/analyze.py` | 完整离线分析器，包括图像读取、ROI、分割、相位展开、标定匹配、统计、绘图、CSV/JSON/Markdown/HTML 输出 |
| `Thickness_variation_analysis/README.md` | 独立工具使用说明和科学边界 |
| `Thickness_variation_analysis/colour_calibration_template.csv` | 四点格式模板；颜色全为零，只能说明字段格式，不能直接用于标定 |
| `Thickness_variation_analysis/example_input.jpg` | 样例图 1，尺寸 `371×374` |
| `Thickness_variation_analysis/example2/...jpg` | 样例图 2，尺寸 `331×259` |
| `Thickness_variation_analysis/example_output/` | 样例图 1 的历史输出 |
| `Thickness_variation_analysis/example2/analysis_output/` | 样例图 2 的历史输出 |

### 2.2 集成实现

| 层级 | 文件 | 作用 |
|---|---|---|
| 核心算法 | `src/vision/thickness_distribution.py` | 无文件 I/O 的内存接口，返回厚度、置信度、掩膜、指标和显示图 |
| GUI 参数与结果面板 | `src/ui/widgets/temporary_measurement.py` | 波长、折射率、CSV、反转、ROI、无膜基准、绝对锚定、标定点采集 |
| GUI 调度 | `src/ui/app.py` | 获取当前矫正帧、裁剪 ROI、异步执行、保存结果、采集基准和颜色标定 |
| 结果弹窗 | `src/ui/widgets/thickness_viewer.py` | 热力图、归一化图、指标和可旋转 3D 曲面 |
| CLI | `src/cli.py` | `michelson analyze thickness-distribution`，输出 JSON 指标 |
| 智能体工具 | `src/agent/device_tools.py` | `thickness_analyze` 只读工具，对当前画面运行默认相对模式 |
| 智能体知识库 | `src/agent/knowledge_base/06_厚度分布标定.md` | 标定流程、公式和字段说明 |
| 测试 | `tests/test_thickness_distribution.py` | 18 个合成图单元测试 |
| 实验标定数据 | `数据记录/颜色光程差标定/` | 60 点正负扫描颜色表及读数换算说明 |

### 2.3 实际数据流

```text
相机当前矫正帧 / 离线图片
        │
        ├─ ROI（GUI 框选或独立版命令行参数）
        │
        ├─ 自动亮区掩膜，或 whole_region=True 使用整个 ROI
        │
        ├─ 相对模式：Lab 色度残差 → atan2 包裹相位 → 二维解包
        │
        └─ 标定模式：像素 Lab → 标定点 KD-tree 最近邻 → 标定量图
                         │
                         ▼
              换算厚度 → 反向可选 → 平滑 → 中位数归零
                         │
          无膜基准图同算法处理并相减 → 再次中位数归零
                         │
                    绝对厚度锚定平移
                         │
          指标、热图、叠加图、CSV、JSON、GUI 3D 显示
```

## 3. 物理理论与正确量纲

### 3.1 透明介质引入的光程差

设透明片局部几何厚度为 (t(x,y))，折射率为 (n)，空气折射率近似为 1。若介质位于迈克尔逊干涉仪一臂且光束近似正入射，光束往返穿过介质两次，则相对于相同几何长度的空气，附加双程光程差为

$$
\Delta\mathrm{OPD}_{\mathrm{plate}}(x,y)=2[n-1]t(x,y).
$$

相位变化为

$$
\Delta\phi(x,y)=\frac{2\pi}{\lambda}\Delta\mathrm{OPD}_{\mathrm{plate}}
=\frac{4\pi(n-1)}{\lambda}t(x,y).
$$

所以从物理相位求厚度应为

$$
t(x,y)=\frac{\lambda}{4\pi(n-1)}\Delta\phi(x,y).
$$

当相位跨越一个完整周期 (2\pi) 时，厚度变化为

$$
\Delta t_{2\pi}=\frac{\lambda}{2(n-1)}.
$$

这正是两套代码在相对模式中使用的步长。默认参数 (lambda=589.3\ \mathrm{nm})、(n=1.523) 时：

$$
\Delta t_{2\pi}=\frac{589.3}{2(1.523-1)}\ \mathrm{nm}
=563.384\ \mathrm{nm}=0.563384\ \mathrm{\mu m}.
$$

该公式本身正确；问题在于代码中的 Lab 色角是否等价于真实干涉相位。当前没有实验或正演仿真证明这种等价关系。

### 3.2 动镜补偿与 20:1 传动比

设动镜真实位移为 (x)。迈克尔逊干涉仪中动镜移动引起的光程差改变为 (2x)。恢复相同中心白光条纹时有

$$
2x=2(n-1)t,
$$

因此

$$
t=\frac{x}{n-1}.
$$

项目把微分表读数差记为 (Delta r)，机械传动比为 (G=20)，即

$$
x=\frac{\Delta r}{20}.
$$

于是

$$
t=\frac{\Delta r}{20(n-1)},
$$

这与 `src/measurement/thickness.py` 的均匀介质厚度公式一致。

这里必须区分三个量：

| 量 | 正确表达 | 建议字段名 |
|---|---:|---|
| 微分表读数差 | (Delta r) | `micrometer_delta_mm` |
| 动镜真实位移 | (x=\Delta r/20) | `mirror_displacement_um` |
| 动镜引起的双程 OPD | (Delta\mathrm{OPD}_{mirror}=2x=\Delta r/10) | `round_trip_opd_um` |

如果 CSV 存的是动镜位移 (x)，则应使用

$$
t=\frac{x}{n-1}.
$$

如果 CSV 存的是通常定义下的双程附加光程差 (Delta\mathrm{OPD})，则应使用

$$
t=\frac{\Delta\mathrm{OPD}}{2(n-1)}.
$$

项目 GUI 自动计算 `|当前−零点|/20×1000`，从量纲和传动关系看，它生成的是动镜位移 (x)，而不是双程 OPD。当前将该列继续命名为 `opd_um`，是独立版与集成版公式分叉的根源。

### 3.3 倾斜入射时的边界

上述推导采用近似正入射。若介质内折射角为 (	heta_t)，精确光程模型会包含倾角、横向光路变化以及插片导致的几何路径变化，不能简单把 (2(n-1)t) 原样使用。当前实现没有入射角参数，因此报告结果只适用于近轴、近正入射的实验条件。

### 3.4 白光颜色与 OPD 并非一一对应

白光干涉像素颜色由光源谱功率、相机三通道光谱响应、曝光、白平衡、两臂强度比、介质色散和 OPD 共同决定。可抽象写成

$$
I_c(\delta)=\int S(\lambda)R_c(\lambda)
\left[I_1(\lambda)+I_2(\lambda)+2\sqrt{I_1I_2}\cos\frac{2\pi\delta}{\lambda}\right]d\lambda,
$$

其中 (c\in\{R,G,B\})。所以：

- RGB/Lab 颜色通常不是 OPD 的全局单值函数；
- 相近颜色可能在不同级次重复出现；
- 曝光、白平衡或光源变化会改变映射；
- 单帧 RGB 在没有空间连续性、零级定位或扫描历史约束时，不能唯一恢复绝对 OPD。

代码 README 对“单帧不能唯一给出绝对厚度”的总体边界判断是正确的。

## 4. 核心算法逐步解析

### 4.1 输入与 ROI

独立版从路径读取 BGR 图，支持 `--roi x,y,width,height`；ROI 最小宽高为 20 像素。集成版接收内存 BGR 帧，GUI 可在视频画面上拖出 ROI。框选后传入 `whole_region=True`，把整个框选区域视为有效样品区。

集成 GUI 使用线程池执行分析，并每 120 ms 轮询结果，避免主界面在相位展开期间冻结。这是合理的界面工程设计。

### 4.2 样品掩膜

`sample_mask()` 的处理顺序为：

1. BGR 转 Lab，取亮度通道 (L)；
2. 高斯模糊，(sigma=2)；
3. 对非暗像素做 Otsu 阈值；
4. 实际阈值为

   $$
   \max\left[8,\min(T_{Otsu},P_{38}(L_{L>3}))\right];
   $$

5. 闭运算，结构元半径约为短边的 1.2%；
6. 3×3 开运算；
7. 只保留最大连通域；
8. 填洞；
9. 向内腐蚀，减少边缘污染。

优点是简单、速度快，适合亮样品/暗背景。限制是：

- 多个分离样品只保留面积最大的一个；
- 纯黑图会退回全图有效，而不是报“无样品”；
- 亮背景或反光可能被误当成样品；
- 框选 ROI 后完全跳过分割，框内背景也进入计算。

### 4.3 相对模式的“色彩相位”

图像转 Lab 后，以 OpenCV 编码中心 128 为零点：

$$
a'=a-128,\qquad b'=b-128.
$$

再用大尺度高斯滤波去除缓慢色偏：

$$
\tilde a=a'-G_{\sigma_c}*a',\qquad
\tilde b=b'-G_{\sigma_c}*b',
$$

其中

$$
\sigma_c=\max\left(5,\frac{\min(H,W)}{18}\right).
$$

包裹色彩坐标定义为

$$
\phi_w=\operatorname{atan2}(\tilde b,\tilde a).
$$

这等价于把 Lab 的色度残差看作二维平面上的旋转向量。它可以描述色带循环，但不是由干涉强度方程直接估计的光学相位，因此报告中应称为“色彩相位代理量”或“循环色彩坐标”。

### 4.4 启发式置信度

色度幅值为

$$
C=\sqrt{\tilde a^2+\tilde b^2}.
$$

灰度局部纹理由灰度减去高斯背景，再对绝对残差平滑得到 (T)。二者分别除以样品区第 90 百分位，最后组合：

$$
q=\operatorname{clip}\left(0.72\frac{C}{P_{90}(C)}
+0.28\frac{T}{P_{90}(T)},0,1\right).
$$

该数值适合用于同图内部加权和平滑，但存在三个解释边界：

- 它是相对图像质量分数，不是正确率概率；
- 不同图像各自归一化，不宜直接横向比较；
- 它没有包含折射率、波长、传动比、配准、标定残差等计量不确定度。

文档 `FEATURES.md` 将“中位置信度”解释成“逐像素估计的平均可信度”也不精确：代码实际计算的是中位数，不是平均值。

### 4.5 二维相位展开与加权平滑

代码使用 `skimage.restoration.unwrap_phase` 对掩膜相位进行二维展开。随后以置信度为权重进行归一化高斯平滑：

$$
\phi_s=\frac{G_\sigma*(q\phi)}{G_\sigma*q},
\qquad
\sigma=\max\left(1.2,\frac{\min(H,W)}{180}\right).
$$

最后减去样品区相位中位数。相对厚度为

$$
t_{rel}=\frac{\phi_s}{2\pi}\cdot
\frac{\lambda}{2(n-1)}.
$$

二维展开隐含“相邻像素真实相位变化通常小于 (pi)”及区域连通、噪声不过强等条件。条纹断裂、低色度区、遮挡或局部梯度过大都可能造成整周错误；一次整周错误在默认参数下约对应 0.563 μm 厚度跳变。

### 4.6 标定模式

标定表读取 `opd_um,r,g,b`，至少四行。标定 RGB 与图像像素都转成 OpenCV Lab，然后使用 `scipy.spatial.cKDTree` 做欧氏最近邻：

$$
j^*(x,y)=\arg\min_j\left\|Lab(x,y)-Lab_j\right\|_2.
$$

每个像素直接取第 (j^*) 个标定量，之后做 3×3 中值滤波。集成版再做默认 (sigma=2) 像素的归一化高斯平滑。

该方案实现简单，但不是沿标定曲线的连续插值，并且完全忽略：

- 标定点的扫描顺序；
- OPD 的正负方向；
- 颜色轨迹自交或周期重复；
- 邻域空间连续性；
- 曝光漂移；
- 标定点重复测量散布。

因此，即使修复当前运行错误，它仍更接近“颜色码本量化”，而不是稳健的绝对 OPD 反演。

### 4.7 二次厚度平滑

集成版新增 `_smooth_thickness()`，默认 (sigma=2) 像素。它对有效厚度和有效掩膜分别高斯滤波，再相除，避免掩膜外零值把边缘拉低。这种归一化卷积是合理的边界处理。

但平滑会改变局部峰谷、梯度和小缺陷尺寸；当前结果中没有记录 `smooth_px`，用户无法从 `summary.json` 追溯平滑强度。独立版没有这一步，因此两套实现即使输入和参数相同，指标也不会完全一致。

### 4.8 无膜基准扣除

集成版可捕获一张无膜基准图。当前图和基准图分别经过完整算法并各自中位数归零，然后做

$$
t_{corr}=t_{sample}-t_{baseline},
$$

再减一次中位数。这能去除两幅图中共同的空间形貌，但不能保留绝对 DC 分量。

目前只检查两图尺寸相同，没有做亚像素配准、曝光一致性检查或掩膜交集质量检查。相机轻微移动、ROI 内物体位置变化或基准捕获后重新对焦，都可能被解释为厚度差。

### 4.9 绝对厚度锚定

GUI 可记录“无膜初始读数”和“中心条纹读数”，按

$$
t_0=\frac{|r_c-r_0|}{20}\cdot\frac{1000}{n-1}\quad(\mu m)
$$

计算厚度锚点，最终执行

$$
t_{anchored}(x,y)=t_{rel}(x,y)+t_0.
$$

这只是给相对形貌增加常数偏置，不会提高局部起伏的准确度，也不会纠正相位展开错误。锚定值自身应传播微分表、传动比、零级定位和折射率的不确定度，目前厚度分布结果没有这条不确定度链。

### 4.10 统计指标

设有效厚度集合为 (V)，分位数为 (P_p(V))。当前指标为：

| 指标 | 代码定义 | 解释 |
|---|---|---|
| 稳健最小/最大 | (P_2, P_{98}) | 排除两端各 2% 像素 |
| 稳健 PV | (P_{98}-P_2) | 不是全图真正 max-min |
| RMS 不均匀度 | 对 ([P_2,P_{98}]) 内数据计算相对其均值的 RMS | 数学上等于裁剪后总体标准差（分母为 (N)） |
| 中间 90% 跨度 | (P_{95}-P_5) | 对极端值更稳健 |
| 中位梯度 | 厚度数值梯度模长的中位数 | 单位 μm/pixel，未换算实际横向长度 |
| 95% 梯度 | 梯度模长的第 95 百分位 | 掩膜边界可能受 NaN 填充值影响 |
| 中位置信度 | (P_{50}(q)) | 启发式图像质量中位数 |

百分比版本使用

$$
t_{ref}=|\operatorname{mean}(V_{2\%-98\%})|,
\qquad
PV_\%=\frac{PV}{t_{ref}}\times100\%.
$$

如果均值非常接近零，仅在小于 (10^{-9}\ \mu m) 时才停用百分比。相对图虽然中位数被强制为零，稳健均值通常并不精确为零，所以会得到极大的无意义百分数。正确策略应是：只有存在独立、正值、可追溯的绝对厚度锚点时才计算百分比，并明确分母使用锚定厚度或经确认的平均绝对厚度。

## 5. 独立版与集成版差异

| 项目 | 独立版 | 集成版 |
|---|---|---|
| 接口 | 文件路径 + argparse | BGR ndarray 纯内存函数 |
| ROI | `--roi x,y,w,h` | 视频鼠标框选；框选后整区有效 |
| 相对算法 | 有 | 有，核心移植自独立版 |
| 标定换算 | `opd_um/[2(n-1)]` | `opd_um/(n-1)` |
| 默认最终平滑 | 无 | 高斯 (sigma=2) px |
| 无膜基准图 | 无 | 有，同算法计算后相减 |
| 绝对锚定 | `--reference-thickness-um` 手填 | 由两次微分表读数自动/手填生成 |
| 输出 | ROI、掩膜、厚度图、归一化图、叠加、截面、置信度、CSV、JSON、MD、HTML | GUI 保存叠加、热图、掩膜、CSV、JSON；弹窗另显示归一化图和 3D，但不保存它们 |
| CLI | 独立脚本 | 项目统一 `michelson` CLI，只输出 JSON 摘要 |
| 智能体 | 无 | 工具只调用默认相对模式，无 CSV、基准、ROI、折射率等参数 |
| 测试 | 无专属测试 | 18 个单元测试，但无标定路径测试 |

值得注意的是，两套实现共同使用字段名 `opd_um`，却采用不同公式。把同一个 CSV 分别交给两套程序，厚度起伏将相差约 2 倍；这不是平滑或统计口径造成的小差异，而是物理量定义不同。

## 6. GUI、CLI 与智能体集成行为

### 6.1 GUI

GUI 提供的功能最完整：

- 波长和折射率输入；
- 标定 CSV 选择；
- 厚度方向反转；
- 鼠标 ROI；
- 捕获/清除无膜基准；
- 初始与中心条纹读数，用于绝对锚定；
- 颜色标定点采集和 CSV 保存；
- 后台线程分析；
- 指标、叠加图、归一化图和 3D 表面显示；
- 自动保存到 `outputs/thickness/<timestamp>/`。

保存目录包括：

- `thickness_overlay.png`；
- `thickness_map.png`；
- `sample_mask.png`；
- `thickness_map_um.csv`；
- `summary.json`。

未保存当前输入帧、无膜基准帧、标定 CSV 副本/哈希、参数全集、置信度图、归一化图和 3D 图，因此结果的复现实验信息不完整。

### 6.2 CLI

统一 CLI 命令为：

```powershell
michelson analyze thickness-distribution image.jpg `
  --wavelength 589.3 `
  --refractive 1.523 `
  --calibration calib.csv `
  --baseline baseline.jpg
```

CLI 只输出模式、步长、波长、折射率和 metrics，不保存逐像素数据或图像。它没有暴露：

- `invert`；
- `reference_thickness_um`；
- `whole_region`/ROI；
- `smooth_px`。

因此 CLI 不能完整复现 GUI 分析设置，且标定模式即使修复后也会因中位数归零而缺少绝对厚度锚点。

### 6.3 智能体工具

`thickness_analyze` 对当前帧直接调用 `analyze_thickness_distribution(frame)`，不接受任何参数。它固定使用：

- 589.3 nm；
- 折射率 1.523；
- 相对模式；
- 自动掩膜；
- 默认 2 px 平滑；
- 无基准、无锚定。

所以智能体能回答当前画面的相对起伏指标，但不能调用 GUI 中已经选择的标定表、基准帧或 ROI。智能体返回的 metrics 又包含可能失真的百分比值，应在接口层屏蔽或标记为不可用。

## 7. 本地复现实验与测试结果

### 7.1 现有单元测试

执行：

```powershell
python -m unittest tests.test_thickness_distribution -v
```

结果：18/18 通过，用时约 0.6 s。覆盖内容包括：

- 返回键与数组尺寸；
- 合成彩虹图产生非零厚度起伏；
- 相对步长公式；
- 反向符号；
- 折射率和空图校验；
- 整 ROI 掩膜；
- 绝对锚点平移；
- 平滑降低梯度；
- 颜色/窄带取色；
- 同图基准相减得到平图；
- 基准图尺寸错误。

这些测试主要验证程序结构和内部自洽性，并没有验证算法恢复已知真实厚度的准确度。

### 7.2 标定模式运行错误

使用仓库正式 60 点 CSV 调用集成函数：

```python
analyze_thickness_distribution(image, calibration=calibration_csv)
```

实际错误：

```text
IndexError: too many indices for array: array is 1-dimensional, but 2 were indexed
```

根因位于两套实现的 `calibrated_opd_map()`：

```python
distance, index = tree.query(lab.reshape(-1, 3), k=1)  # distance: (H*W,)
scale = np.percentile(distance[mask], 90)               # mask: (H,W)
```

应先执行 `distance_map = distance.reshape(mask.shape)`，再用 `distance_map[mask]`。代码后面其实已经对 `distance` 做了 reshape，但发生在出错语句之后。当前测试没有构造临时 CSV 并运行标定路径，所以未发现该错误。

### 7.3 两张仓库样例的当前集成相对模式结果

在默认参数和默认 2 px 平滑下，本地得到：

| 样例 | 有效像素 | 步长 / μm | 稳健 PV / μm | RMS / μm | 稳健均值 / μm | 当前 PV 百分比 |
|---|---:|---:|---:|---:|---:|---:|
| `example_input.jpg` | 90,132 | 0.563384 | 2.017701 | 0.454263 | 0.089796 | 2246.971% |
| `example2/...jpg` | 40,921 | 0.563384 | 2.470872 | 0.624811 | -0.010430 | 23690.051% |

这两个百分比并不表示样品真的有数千或数万百分比的不均匀度，而是相对图被归零后仍拿接近零的稳健均值作分母。该实验直接证明当前“只有分母小于 (10^{-9}) 才禁用百分比”的判据不足。

### 7.4 历史样例输出的解释

独立版已有样例输出把相对图平移到 176.7 μm，样例 1 的历史 `summary.json` 给出：

- 中位数 176.7 μm；
- 稳健 PV 2.0290 μm；
- RMS 0.4627 μm；
- 中位置信度 0.3012。

176.7 μm 是人为提供的常数锚点，不是单帧图像自动恢复的绝对厚度。PV 和 RMS 来自相对形貌算法；两类信息应在报告中分开注明来源。

## 8. 主要问题与风险分级

### P0：标定模式当前不可运行

二维掩膜索引一维距离数组，独立版和集成版都会失败。这使 README、GUI 和 CLI 宣称的标定路径在当前提交上不可用。

建议：立即修复 reshape 顺序，并增加两套实现的端到端标定测试。

### P0：`opd_um` 量纲语义与公式冲突

GUI 自动值是动镜位移 (x=\Delta r/20)，集成版按 (x/(n-1)) 计算在补偿模型下是自洽的；但字段和文档称其为 OPD。独立版则按双程 OPD 解释同名字段。错误使用 CSV 会产生 2 倍系统误差。

建议：废弃歧义字段。新格式至少加入：

```csv
mirror_displacement_um,scan_direction,r,g,b
```

或明确使用：

```csv
round_trip_opd_um,scan_direction,r,g,b
```

并在文件元数据中记录传动比和量定义。旧 `opd_um` 必须通过版本字段迁移，不能静默猜测。

### P0：标定输出仍被强制归零

标定映射之后 `_thickness_map()` 无条件减去中位数。因此标定 CSV 自身的绝对零点被删除。没有 `reference_thickness_um` 时，所谓“接近绝对厚度”并不成立。

建议：明确拆成 `relative_variation_um` 与 `absolute_thickness_um` 两条输出；只有用户选择“相对形貌”时归零。标定模式是否保留绝对 DC 分量应由清晰参数控制。

### P1：自动标定丢失方向

`abs(current-zero)` 使零点两侧都成为正值，无法生成仓库正式表中的负 OPD/位移。对于周期颜色，方向是消除级次歧义的重要信息。

建议：保留带符号差值，记录电机/微分表方向，并强制单调扫描；必要时正向和反向分别建表以评估回程差。

### P1：最近邻颜色码本不能解决周期歧义

相同或相近 Lab 颜色可对应多个标定位置。KD-tree 只返回某一个最近点，没有利用扫描次序或像素空间连续性。3×3 中值和 2 px 高斯只能美化量化块，不能恢复正确级次。

建议：把标定建模为带顺序的一维颜色轨迹，先投影到轨迹弧长，再结合零级锚点、相位展开和邻域连续性选择分支。更稳健的方向是直接拟合 RGB 多波长干涉正演模型或使用扫描式相干包络定位。

### P1：无锚定百分比指标会误导

当前样例已出现 2,247% 和 23,690%。GUI 和智能体均会显示或返回这些值。

建议：只有 `reference_thickness_um` 明确存在且大于其不确定度阈值时才计算百分比；否则返回 `None`，界面显示“未锚定，不适用”。

### P1：基准相减没有图像配准和质量门

仅尺寸相同不足以保证像素对应。轻微位移会把条纹位置差转成厚度差。

建议：保存基准原图；相减前用稳定结构做刚性/仿射配准；报告配准残差；以掩膜交集作为有效区域；曝光或白平衡变化时拒绝分析。

### P1：没有计量学验证和不确定度预算

现有测试只检查软件行为，没有使用已知楔角、台阶高度、标准厚度片或可追溯轮廓仪结果做准确度验证。

建议：分别报告重复性、偏差、非线性、空间分辨率、跨日稳定性和不确定度，不要用启发式 confidence 代替。

### P2：独立版与集成版发生算法漂移

集成版新增默认平滑、基准、锚定和 3D；独立版仍保留不同标定公式和不同输出。复制代码造成维护分叉。

建议：核心算法只保留一个库实现；独立脚本改为调用该公共 API，报告/绘图作为独立适配层。

### P2：参数与原始证据保存不全

集成输出没有保存输入帧、基准帧、标定表副本/哈希、ROI 原坐标、平滑参数、软件提交号、相机曝光/白平衡和时间戳对应关系。

建议：增加 `run_manifest.json`，保证每个数值结果都能追溯到输入和配置。

### P2：热图和梯度细节

- `heatmap_bgr()` 的注释称 NaN 为灰色，实际把 NaN 填成稳健下限并映射为色图低端；
- 梯度计算先把 NaN 填成全图中位数，掩膜内边缘像素可能产生人为梯度；
- 横向单位只有 pixel，没有像素尺寸或放大率，不能解释为真实斜率。

建议：显式把掩膜外热图设灰；梯度只在有效邻域内计算；加入横向标定后输出 μm/mm 或无量纲坡度。

### P3：报告中的固定现象描述可能超出证据

独立版自动报告固定写入“条纹明显弯曲且局部间距变化”等句子，无论输入图是否真的满足。该结论没有由代码中的曲率或间距统计触发。

建议：要么改成条件式解释指南，要么增加可验证的曲率/局部频率指标后再自动生成结论。

## 9. 推荐修复路线

### 第一阶段：恢复正确、可测试的基本功能

1. 修复标定距离数组 reshape 错误；
2. 增加最小临时 CSV 的端到端测试；
3. 统一物理量命名，迁移 `opd_um`；
4. 去掉自动标定的绝对值，记录扫描方向；
5. 标定模式和相对模式明确决定是否归零；
6. 未锚定时禁止百分比指标；
7. 把独立脚本改为调用公共核心，消除公式分叉。

### 第二阶段：提高算法可信度

1. 建立有序颜色轨迹而非无序 KD-tree 码本；
2. 对标定点做重复采集，估计颜色散布和回程差；
3. 加入曝光/白平衡锁定检查；
4. 基准图相减前做配准；
5. 输出低置信度掩膜并允许阈值剔除，而不只是把置信度用于平滑；
6. 把相位展开残差、枝切/不连续区域作为质量指标；
7. 保存全部运行证据和参数。

### 第三阶段：完成计量验证

建议设计以下验证样品和实验：

| 验证项目 | 样品/参考 | 主要指标 |
|---|---|---|
| 零输入 | 无膜或光学平板 | 零偏、空间残差、假 PV |
| 已知台阶 | 可追溯台阶高度标准 | 偏差、重复性、边缘响应 |
| 已知楔片 | 已知楔角或轮廓仪参考 | 线性斜率、方向、空间非线性 |
| 均匀厚度片 | 多个标称厚度 | 绝对误差、折射率敏感性 |
| 重复装夹 | 同一样品多次装夹 | 再现性、配准敏感性 |
| 曝光扰动 | 固定样品改变曝光/白平衡 | 颜色映射鲁棒性 |
| 跨日标定 | 多日重复标定 | 漂移和重新标定周期 |

对于每一验证点，至少区分：

- 算法重复性；
- 仪器重复性；
- 标定偏差；
- 折射率与色散不确定度；
- 波长/光谱不确定度；
- 传动比和微分表不确定度；
- 配准误差；
- 空间采样与平滑引入的分辨率损失。

### 第四阶段：产品化判据

只有满足以下条件，界面才应显示“绝对厚度分布”：

1. 标定文件量定义和版本明确；
2. 标定覆盖当前颜色范围，且距离/残差通过质量门；
3. 曝光、白平衡、相机和光源配置与标定一致；
4. 基准图已配准且质量通过；
5. 绝对锚点存在并有不确定度；
6. 相位/颜色分支无未解决歧义；
7. 输出附带不确定度或至少验证误差范围；
8. 使用标准样品的验证结果在规定允差内。

否则应降级显示为“相对厚度起伏”或“定性形貌”。

## 10. 建议的数据结构与 API

建议把当前返回字典升级为显式区分物理含义的结构：

```python
{
    "result_kind": "relative_variation" | "calibrated_variation" | "absolute_thickness",
    "relative_variation_um": ...,       # 始终零中心，可选
    "absolute_thickness_um": ...,        # 只有满足条件时存在
    "quality_score": ...,                # 明确不是 uncertainty
    "valid_mask": ...,
    "metrics": {
        "robust_pv_um": ...,
        "rms_nonuniformity_um": ...,
        "relative_pv_percent": None,     # 无绝对锚定时必须为 None
    },
    "provenance": {
        "input_image_sha256": ...,
        "baseline_image_sha256": ...,
        "calibration_sha256": ...,
        "calibration_quantity": "mirror_displacement_um",
        "wavelength_nm": ...,
        "refractive_index": ...,
        "gear_ratio": 20.0,
        "roi_xywh": ...,
        "smooth_sigma_px": ...,
        "git_commit": ...,
    },
    "warnings": [...]
}
```

对于标定文件，建议采用带元数据的 JSON/YAML，或在 CSV 旁附 manifest：

```yaml
schema_version: 2
quantity: mirror_displacement_um
gear_ratio: 20.0
signed: true
scan_direction: increasing
camera:
  exposure: <实测值>
  white_balance: <实测值>
source:
  light: <实测光源>
columns: [mirror_displacement_um, r, g, b]
```

## 11. 对现有输出的正确解读

在当前版本中，可以合理使用的结论是：

- 热图可用于观察模型推断出的相对厚薄趋势；
- 稳健 PV、RMS 和 P5–P95 跨度可用于同一装置、同一参数、同一图像条件下的内部比较；
- 低置信度区域应回看原图，检查背景、反光、断纹和低色度；
- 反转选项只改变符号约定，不提供物理方向证据；
- 人工锚定只改变整体偏置，不改善局部准确度。

当前不应直接作出的结论是：

- “单张图已测得可溯源绝对厚度”；
- “置信度 0.8 表示 80% 正确”；
- “相对模式百分比就是实际厚度不均匀率”；
- “热图局部跳变一定是真实缺陷”；
- “标定模式已经在当前提交可正常使用”；
- “同一个 `opd_um` CSV 在独立版和集成版含义相同”。

## 12. 证据索引

本报告的主要判断可在以下本地文件定位：

| 证据 ID | 文件与定位 | 支持内容 |
|---|---|---|
| E01 | `src/vision/thickness_distribution.py:1-13` | 集成算法边界及两种模式公式 |
| E02 | `src/vision/thickness_distribution.py:35-168` | 掩膜、色彩相位、置信度、解包和标定最近邻实现 |
| E03 | `src/vision/thickness_distribution.py:181-230` | 指标和百分比定义 |
| E04 | `src/vision/thickness_distribution.py:252-312` | 平滑、中位数归零及两种厚度换算 |
| E05 | `src/vision/thickness_distribution.py:315-397` | 基准相减、锚定、归一化和返回值 |
| E06 | `Thickness_variation_analysis/analyze.py:135-165,370-428` | 独立版标定错误、公式和输出流程 |
| E07 | `Thickness_variation_analysis/README.md:1-79` | 独立版科学边界和 CSV 语义 |
| E08 | `src/ui/app.py:3851-3951` | GUI 异步分析、参数和保存文件 |
| E09 | `src/ui/app.py:3960-4116` | 基准捕获、绝对锚定、标定取色和自动量计算 |
| E10 | `src/ui/widgets/temporary_measurement.py:297-509` | GUI 对外参数、公式文字和标定界面 |
| E11 | `src/ui/widgets/thickness_viewer.py:29-239` | 热图、归一化图、指标和 3D 显示 |
| E12 | `src/cli.py:93-109,194-201` | CLI 暴露参数和 JSON 输出 |
| E13 | `src/agent/device_tools.py:176-185,338-357` | 智能体默认相对模式及工具注册 |
| E14 | `tests/test_thickness_distribution.py:1-164` | 当前 18 个测试及标定测试缺口 |
| E15 | `src/measurement/thickness.py:12-27` | 20:1 传动下的均匀厚度公式 |
| E16 | `数据记录/颜色光程差标定/读数对应关系.md` | 60 点正负标定、读数与修正值关系 |
| E17 | `src/agent/knowledge_base/01_原理.md:9-20` | 动镜位移引起双程光程变化 |
| E18 | `报告和PPT/测盖玻片厚度实验/盖玻片厚度_不确定度分析模型.md:10-36` | (2(n-1)t) 与条纹级次的项目内理论推导 |
| E19 | Git 提交 `3e1d0c8`、`8bef36f` | `opd_um` 曾在“动镜位移/OPD”之间改名，公式分叉的历史来源 |

## 13. 最终评价

项目已经完成了一个结构较完整的厚度分布原型：从单图处理、ROI、基准、标定采集，到 GUI 异步执行、结果保存、CLI 和智能体入口均已搭建，工程集成度较高。相对模式的公式链在“色彩代理相位等价于真实相位”的假设下内部自洽，掩膜、加权平滑和稳健统计也具备实际图像处理价值。

但“绝对/标定厚度”链仍处于原型阶段：当前代码无法运行标定路径，物理量命名存在 2 倍因子歧义，绝对分量被中位数归零，颜色周期歧义未解决，也没有标准样品验证和不确定度预算。优先完成 P0/P1 修复后，这项功能可以成为可靠的相对均匀度分析工具；要升级为定量绝对厚度测量，还需有序标定、图像配准、计量验证和完整结果溯源。
