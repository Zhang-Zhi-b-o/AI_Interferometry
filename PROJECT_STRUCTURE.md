# 迈克尔逊干涉仪智能辅助系统

## 项目概述

基于 YOLO 实时检测 + 步进电机自动控制的迈克尔逊干涉实验辅助软件。摄像头拍摄干涉条纹，AI 实时检测并自动调整电机，未来将集成智能体辅助教学。

---

## 功能模块

### 一、摄像头与画面控制

**做什么**：
- 连接 USB 摄像头，实时采集干涉仪观察屏画面
- 手动旋转角度校正（±任意角度）
- 画面缩放（1.0× ~ 8.0×）
- 鼠标拖拽平移画面

**何时用**：实验全程运行，是系统的眼睛。

---

### 二、YOLO 实时检测与预测

**做什么**：
- 加载 YOLOv8 模型，对画面进行实时目标检测
- 支持鼠标拖拽框选 ROI 区域，仅对 ROI 内进行检测
- 检测类别：color（彩色条纹）、black（黑色条纹）
- 实时显示检测目标数、各类别置信度、电机建议
- 单帧预测弹出独立窗口，支持右键保存标注图片

**何时用**：需要对干涉条纹进行 AI 分析时开启。

---

### 三、电机控制

**做什么**：

| 模式 | 说明 |
|------|------|
| **手动转** | 设定速度（1~10档），启动/停止/加速/减速/查询状态 |
| **边转边识别** | 电机持续旋转，检测到彩色条纹自动减速，检测到黑色条纹锁定 |
| **短转后分析** | 转一段→暂停分析→再转，检测到黑条自动锁定 |

**何时用**：连接步进电机后使用，自动模式下配合 YOLO 检测实现闭环控制。

---

### 四、视频录制

**做什么**：
- 录制预览画面或相机原始画面
- 可设帧率，输出 AVI/MP4

**何时用**：需要存档实验过程时开启。

---

### 五、实时状态监控

**做什么**：
- FPS 帧率显示
- 电机连接状态（端口号）
- 电机转速（deg/s）
- 电机当前档位

**何时用**：始终显示，了解系统运行状态。

---

## UI 布局

```
┌──────────────────────────────────────────────────┐
│  状态栏（置顶固定）                                │
├────────────┬─────────────────────────────────────┤
│ 插件开关栏  │                                     │
│ ☑状态      │                                     │
│ ☑摄像头    │         视频画面                      │
│ ☑模型预测  │    （实时预览 / YOLO检测框叠加）       │
│ ☑视频录制  │    鼠标拖拽 → 框选ROI                  │
│ ☑电机控制  │    鼠标拖拽 → 平移画面                  │
│ ☑运行日志  │    右键 → 保存图片（单帧模式）           │
│            │                                     │
│ 可折叠面板  │                                     │
│ ▾ 实时状态 │                                     │
│ ▾ 摄像头   │                                     │
│ ▾ 模型预测 │                                     │
│ ▾ 视频录制 │                                     │
│ ▾ 电机控制 │                                     │
│ ▾ 运行日志 │                                     │
└────────────┴─────────────────────────────────────┘
```

---

## 模块关系

```
                    ┌──────────────┐
                    │   YoloCamApp │  ← 主控制器（组装一切）
                    └──────┬───────┘
        ┌──────────┬────────┼────────┬──────────┐
        ▼          ▼        ▼        ▼          ▼
   摄像头插件   模型插件   录制插件  状态面板   电机插件
        │          │
   ┌────▼────┐ ┌──▼──────────┐
   │ Camera  │ │ YOLODetector│
   │ Manager │ │ + 类别映射   │
   └─────────┘ │ + FrameCorr │
               └──────┬──────┘
                      │
               ┌──────▼──────┐
               │MotorController│
               └─────────────┘
```

---

*最后更新：2026-06-26*
