# 2025 年往届项目：实验辅助智能体相关方案分析

## 1. 分析范围与方法

资料来源为 `D:\Files\Work\光电\资源\2025`。为减少无关内容与大模型 token 消耗，本次没有全文逐份阅读全部 300 余个 PDF，而是采用以下流程：

1. 根据文件名筛选“智能体、智能助手、自然语言交互、辅助教学、探究伙伴”等项目；
2. 优先读取研究报告，演示 PPT 仅作为视觉补充来源；
3. 对 PDF 逐页做本地文本检索，只提取“知识库、RAG、大模型、AI Agent、多模态、实验指导、报告批改、数据诊断”等命中页；
4. 优先分析与迈克尔逊干涉实验直接相关的方案，再用其他实验的智能体方案补充设计思路。

重点分析了以下项目：

| 项目 | 相关性 | 主要智能体形态 |
| --- | --- | --- |
| 东华大学：基于 LSTM 图像处理与自然语言交互的迈克尔逊干涉实验智能助手 | 很高 | 文本/图像多模态实验助手 |
| 北京航空航天大学杭州校区：可回溯式数控迈克尔逊干涉仪及智能体辅助教学 | 很高 | 课前、课中、课后教学智能体 |
| 哈尔滨工业大学：AI 赋能迈克耳孙干涉仪高精度波长测量与智能实验辅助系统 | 很高 | 图像识别加规则知识库专家系统 |
| 北京邮电大学：“AI 物理探究伙伴”智析实验平台 | 较高 | 实验数据理解与探究引导 Agent |
| 西南石油大学：AI 赋能的牛顿环实验自动化测量与辅助教学系统 | 中等 | 图像问题反馈与报告评阅助手 |
| 南京理工大学紫金学院：基于人工智能的牛顿环实验辅助测量系统 | 补充 | “错误类型-解决方案”知识库 |

## 2. 往届方案归纳

### 2.1 东华大学：多模态实时实验助手

该项目和当前 `AI_Interferometry` 最接近。

报告提出一个三层架构：

```text
Web 前端（HTML/CSS/JavaScript）
        ↓ POST /ask
Python Flask 后端
        ↓ 文本问题 + 当前相机帧
GLM-4V-Flash / GLM-4-Flash
```

核心功能包括：

- 回答迈克尔逊干涉原理、装置操作、实验现象和数据处理问题；
- 自动获取当前实验图像，缩放到 `640×480` 后进行 Base64 编码；
- 将图像与用户问题一并发给视觉语言模型；
- 视觉模型先理解实验图像，文本模型再完成解释和指导；
- 使用迈克尔逊干涉专用提示词、结构化知识库和常见问题模板约束回答；
- API 超时、图像缺失时进行降级处理，并提供友好的错误提示；
- 通过独立线程启动 Flask 服务，避免阻塞视觉和电机控制程序；
- 另外使用 Vosk 做本地语音命令识别，实现“启动电机、加速、设置目标”等控制。

值得复用的设计：

1. 智能体能看到当前实验画面，而不是只做通用问答；
2. 将“语音硬件控制”和“开放式大模型问答”分开处理；
3. 后端独立线程运行，不阻塞实时图像与控制流程；
4. 视觉模型不可用时可以退化为文本问答；
5. 用领域提示词和知识库限制回答范围。

局限：

- 报告主要描述结构化知识库，但没有明确给出完整的 RAG 检索、切片和引用机制；
- 直接把图片传给云端模型会产生较高 token/带宽成本；
- 若允许大模型直接发电机指令，存在误操作风险；
- 未展示对回答事实性、幻觉率和操作安全性的系统评估。

来源：研究报告 PDF 第 2、7、28-31、35 页。

### 2.2 北京航空航天大学杭州校区：教学全流程智能体

该项目基于通义千问 Qwen-Plus，并部署在微信小程序中。智能体覆盖三个教学阶段：

- 课前：生成实验预习题，辅助学生掌握基础原理；
- 课中：自然语言答疑、识图、实验指导；
- 课后：批改实验报告，给出评分与改进建议；
- 扩展方向：把实验数据和报告导出成 PDF，供师生查阅和归档。

该方案的最大价值不是模型本身，而是把智能体放入完整教学闭环：

```text
课前预习 → 课中指导 → 数据采集 → 课后报告 → 反馈改进
```

项目还把智能体与 Phyphox、ESP32 和实验硬件协同起来，使其不仅回答理论问题，还能使用实验过程数据提供反馈。

值得复用的设计：

- 给智能体划分明确的教学阶段和角色；
- 将预习、操作指导、结果分析、报告评阅拆成独立功能；
- 保存实验记录，使回答和评价可以追溯；
- 面向学生和教师提供不同输出。

局限：

- 微信小程序会增加部署和网络依赖；
- 自动出题和报告评分需要标准答案、评分量表和教师复核；
- 单纯使用通用大模型不等于具备可靠的实验状态理解能力。

来源：研究报告 PDF 第 2、13、22-25 页。

### 2.3 哈尔滨工业大学：图像识别加规则专家系统

该项目没有把所有任务交给大语言模型，而是采用更可控的专家系统：

```text
CCD 图像
  ↓
ResNet 条纹类型识别
  ↓
领域知识库/经验规则
  ↓
形成机理解释 + 调节建议
```

知识库内容分为两部分：

1. 物理原理，如等倾干涉光程差、等厚干涉和楔角关系；
2. 操作经验，如圆条纹中心偏移对应动镜倾角误差、直条纹对比度不足可能对应光程差均匀性问题。

系统每 2 秒采集和分析一次图像，形成“图像感知-智能分类-知识推理-调节指导”的闭环。

值得复用的设计：

- 对安全和确定性要求高的诊断优先使用规则，而不是自由生成；
- 将图像识别结果转换成结构化状态，再交给知识模块解释；
- 操作建议可以逐条关联证据和规则，便于追溯；
- 很适合作为大模型调用失败时的离线降级方案。

局限：

- 规则覆盖范围有限，需要持续维护；
- 条纹分类错误会直接影响后续诊断；
- 难以处理规则库未覆盖的开放式问题。

来源：研究报告 PDF 第 26-27、39 页。

### 2.4 北京邮电大学：数据理解型探究伙伴

该方案强调智能体不应停留在表层问答，而应理解学生上传的实验数据与拟合结果。

其能力包括：

- 识别系统误差、随机误差和理论模型偏差；
- 根据当前实验进度给出下一步实验优化建议；
- 使用 PySR 等算法发现符号规律，再由 Agent 解释物理意义；
- 从单摆验证引导到大角度摆、复杂摆和混沌系统；
- 形成“测量-分析-探索”的循环，而不是只给标准答案。

对当前项目的启发是：智能体应读取真正的实验上下文，例如：

- 当前 YOLO 类别和置信度；
- 中心条纹坐标和稳定性；
- 电机模式、速度和停止原因；
- 千分尺位移记录；
- 多次测量结果、拟合残差和不确定度；
- 当前实验处于调节、寻零、测量还是分析阶段。

只有读取这些状态，智能体才可能给出与当前实验有关的诊断，而不是泛泛介绍迈克尔逊干涉原理。

局限：

- 报告对 Agent 的具体工具调用协议和数据安全机制说明不足；
- 自动生成研究方向需要防止把模型猜测包装成实验结论；
- 高阶诊断必须展示数据依据、计算过程和不确定度。

来源：研究报告 PDF 第 7、11-16 页。

### 2.5 牛顿环项目：图像问题反馈与报告评阅

西南石油大学的方案将 OpenCV 检测结果、DeepSeek 模型和 PC/移动端协同结合：

- 识别实验图像中的典型调节问题；
- 根据识别结果生成针对性操作指导；
- 解析学生实验数据并自动评分；
- 检查有效数字、单位、数据完整性和步骤逻辑；
- 输出个性化改进建议。

南京理工大学紫金学院则采用更简单的“错误类型-解决方案”知识库，用于把检测到的调焦、偏心、空回等问题映射为明确操作建议。

这说明报告评阅最好采用混合方式：

```text
确定性检查：单位、有效数字、缺失数据、公式、阈值
                         +
大模型评价：表达、逻辑、误差讨论、改进建议
```

不应让大模型独立决定所有分数。

来源：西南石油大学研究报告 PDF 第 2、35、46 页；南京理工大学紫金学院研究报告 PDF 第 36 页。

## 3. 共性架构

往届项目中比较成熟的实验智能体通常包含五层：

```text
交互层
文本、图片、语音、快捷问题
        ↓
实验上下文层
相机帧、检测结果、电机状态、测量记录、实验阶段
        ↓
知识与工具层
领域知识库、计算器、不确定度算法、规则诊断、报告解析
        ↓
推理层
大语言模型/视觉语言模型
        ↓
安全与输出层
证据引用、操作确认、权限限制、日志、降级处理
```

## 4. 对当前项目的推荐方案

### 4.1 第一版目标

建议第一版只实现三个高价值功能：

1. **实验问答**：回答原理、调节方法、常见故障和数据处理问题；
2. **状态诊断**：结合当前检测结果、电机状态和中心条纹状态给出建议；
3. **结果分析**：读取用户选定的测量记录，完成公式计算、误差检查和不确定度解释。

第一版暂不实现：

- 大模型直接控制电机；
- 无人工确认的自动寻零；
- 完全自动评分；
- 每帧调用多模态大模型。

### 4.2 推荐的软件结构

当前项目已经预留 `src/agent`，建议扩展为：

```text
src/agent/
├── service.py          # 智能体统一入口
├── context.py          # 汇总当前实验状态
├── prompts.py          # 系统提示词与任务模板
├── knowledge.py        # 本地知识库检索
├── tools.py            # 计算、不确定度、状态查询等工具
├── safety.py           # 权限和输出检查
├── providers/
│   ├── base.py
│   └── deepseek.py     # 对接 config.yaml 中已有的 DeepSeek 配置
└── knowledge_base/
    ├── 原理.md
    ├── 操作流程.md
    ├── 故障诊断.md
    ├── 数据处理.md
    └── 安全规范.md
```

UI 可增加一个“实验助手”插件面板：

- 对话历史；
- 文本输入；
- “附加当前画面”复选框；
- “附加实验状态”复选框；
- 原理解释、调节建议、分析记录等快捷按钮；
- 回答中的知识来源与时间戳；
- 清空对话和导出记录。

### 4.3 实验上下文格式

不要把所有日志和图片一直塞进提示词。每次只生成紧凑的结构化上下文：

```json
{
  "stage": "zero_search",
  "camera": {"running": true, "fps": 10.8},
  "vision": {
    "model": "best.pt",
    "classes": {"zero_order": 0.83, "near_fringe": 0.51},
    "center_x_px": 641.2,
    "center_confidence": 0.76
  },
  "motor": {
    "connected": true,
    "mode": "step",
    "gear": 5,
    "auto_state": "pause"
  },
  "measurement": {
    "micrometer_mm": 12.205,
    "records": 6
  }
}
```

### 4.4 最节省 token 的运行策略

1. 普通知识问答只发送用户问题和检索到的 2-4 个短知识片段；
2. 只有用户明确询问当前实验状态时才附加结构化状态；
3. 只有用户选择“附加当前画面”时才调用视觉模型；
4. 同一问题优先命中本地 FAQ 和规则库，不调用大模型；
5. 对话历史定期压缩成实验摘要，不重复发送全部消息；
6. 图像先由本地 YOLO/OpenCV 提取特征，默认只把特征发给文本模型；
7. 数据分析由本地 Python 工具完成，大模型只负责解释结果；
8. 知识块按标题和主题切分，避免把整篇报告放入上下文；
9. 缓存相同问题和相同实验状态下的回答；
10. 设置最大检索片段数、最大历史轮数和最大输出长度。

建议的请求优先级：

```text
规则/FAQ → 本地计算工具 → 文本 LLM → 视觉 LLM
低成本                                      高成本
```

### 4.5 安全边界

智能体默认只能读取状态，不能直接操作硬件。

若未来增加工具调用，应采用三级权限：

| 权限 | 示例 | 是否需要确认 |
| --- | --- | --- |
| 只读 | 查询 FPS、模型结果、电机状态、测量记录 | 否 |
| 低风险 | 保存截图、导出报告、切换显示面板 | 一般不需要 |
| 硬件控制 | 启停电机、改变速度、自动寻零 | 必须二次确认 |

禁止智能体绕过现有电机安全状态机。即使模型建议移动，也必须由原有控制层验证串口连接、最大运行时间、目标丢失和停止条件。

## 5. 建议的开发阶段

### 阶段一：本地知识助手

- 建立 5 个 Markdown 知识文件；
- 实现关键词/全文检索和 FAQ；
- 接入 DeepSeek 文本模型；
- 回答附带本地知识来源；
- 不连接相机和电机控制。

### 阶段二：实验状态感知

- 实现 `ExperimentContext`；
- 读取相机、YOLO、中心条纹、电机和测量记录状态；
- 增加规则诊断；
- 智能体只给建议，不发控制指令。

### 阶段三：数据分析与报告

- 将公式计算和不确定度计算封装为本地工具；
- 自动检查单位、有效数字、异常值和数据完整性；
- 生成实验摘要和报告草稿；
- 保留原始数据、计算过程和引用来源。

### 阶段四：受控多模态与教学闭环

- 用户主动选择时上传当前帧；
- 增加预习题、课中指导和课后报告评阅；
- 建立教师评分量表和人工复核流程；
- 对回答准确率、诊断成功率、token 成本和响应延迟进行评估。

## 6. 验收指标建议

| 类别 | 指标 |
| --- | --- |
| 知识问答 | 标准题正确率、引用命中率、无依据回答比例 |
| 状态诊断 | 故障识别准确率、漏报率、误报率 |
| 数据分析 | 与人工计算结果的一致性、单位与有效数字检查准确率 |
| 安全性 | 未确认硬件操作次数必须为 0 |
| 性能 | 文本响应时间、视觉响应时间、失败降级成功率 |
| 成本 | 单次问答输入/输出 token、视觉调用比例、缓存命中率 |
| 教学效果 | 预习正确率、实验完成时间、学生满意度、教师复核差异 |

## 7. 结论

往届项目表明，适合本项目的智能体不应只是一个聊天框。最有价值的组合是：

```text
本地知识库
+ 当前实验状态
+ 确定性计算工具
+ 规则专家系统
+ 受控的大语言模型解释
```

对 `AI_Interferometry` 而言，建议以“低 token 的文本知识助手”为起点，再逐步加入状态诊断、数据分析和按需多模态。硬件控制继续由现有确定性状态机负责，智能体只提供解释和经用户确认的建议。这样既能借鉴往届项目的亮点，也能避免高成本、幻觉和误操作问题。

